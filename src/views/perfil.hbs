<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
    <title>Wheelie Bikes</title>
</head>
<body>
    <header>
        {{> navbar }}
    </header>
    <main class="container container-layout">
        <h4 class="g-4">¡Hola {{{user.nombre}}} !</h4>
        <h5 class="my-2">Tus datos</h5>
        <p>Correo: {{{user.username}}} </p>
        <p>Dirección: {{{user.direccion}}}</p>
        <p>Teléfono: {{{user.telefono}}}</p>
        {{#if userOrder }}
        <h5 class="my-2">Tus pedidos</h5>
        <div class="align-middle">
                <table class="table table-secondary table-striped container-layout text-center">
                    <thead>
                        <tr>
                            <th scope="col">Fecha</th>
                            <th scope="col">Pedido</th>
                            <th scope="col">Estado</th>
                            <th scope="col">Dirección de Entrega</th>
                            <th scope="col">Total</th>
                            <th scope="col">Ver Detalle</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each userOrder}}
                                <tr>
                                    <td scope="row">{{timestamp}}</td>
                                    <td>{{pedido}}</td>
                                    <td>{{estado}}</td>
                                    <td>{{direccion}}</td>
                                    <td>USD$ {{total}}</td>
                                    <td>
                                        <a href=/orden/{{pedido}} class="btn btn-success">
                                            <span class="material-symbols-outlined">
                                                search
                                            </span>
                                        </a>
                                    </td>
                                </tr>
                        {{/each}}
                    </tbody>
                </table>
        </div>
        {{else}}
        <h5 class="g-4">No hay pedidos.</h5>
        {{/if}}
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/normalizr@3.6.1/dist/normalizr.browser.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/index.js"></script>
</body>
</html>